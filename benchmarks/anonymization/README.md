# Anonymization benchmark

Structure:
- `input/`   benchmark input files
- `output/`  anonymized output files
- `reports/` analysis reports and comparisons

Recommended workflow:
1. Place synthetic benchmark input into `input/`
2. Save anonymizer output into `output/`
3. Compare versions and store notes in `reports/`
